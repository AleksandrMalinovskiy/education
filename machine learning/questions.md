Jupyterhub 
    - Jupyter — интерактивный блокнот, первоначально являвшийся веб-реализацией и развитием IPython, ставший самостоятельным проектом,
     ориентированным на работу со множеством сред выполнения — не только Python, но и R, Julia, Scala и ряда других.
    - JupyterHub  это тот же Jupyter, только ставится он на отдельный сервер и работает как клиент-серверное веб-приложение.

mlflow 
    - Mlflow Tracking  для регистрации экспериментов и показателей
    
    * При обучении используется библиотека.  
    * По итогу получаем модели. 
    * В процессе обучения мы получаем набор метрик парамеров артифактов и т.д. которые мы логируем в результате этих экспирментов
    * Для восроизводимости мы получаем целый проект который можно повторно воспроизвести 
    * И возможен деплой в кубер докер


    - MLflow Projects Это формат упаковки кода для многократного использования и воспроизведения эксперимента.
    - MLflow Models Это формат для упаковки моделей машинного обучения, что позволяет использовать модель как сервис. С помощью MLflow     
     Models можно упаковывать модель в Docker-образ для последующего использования в Kubernetes.
    - MLflow Registry Этот централизованное хранилище моделей. Оно включает в себя UI, который позволяет управлять полным жизненным циклом 
    модели. Также он позволяет сравнивать разные модели между собой, например, чтобы увидеть отличия в параметрах.
    
    Состовляющие 
       - mlflow
       - postgres
       - s3 бакет
    

Airflow
    - Directed Acyclic Graph это некоторое смысловое объединение ваших задач, которые вы хотите выполнить в строго определенной
     последовательности по определенному расписанию. Airflow представляет удобный web-интерфейс для работы с DAG’ами и другими сущностями:
    - Airflow Operator то сущность, на основании которой создаются экземпляры заданий, где описывается, что будет происходить во время 
    исполнения экземпляра задания. Релизы Airflow с GitHub уже содержат набор операторов, готовых к использованию
    - Планировщик Планировщик задач в Airflow построен на Celery. Celery — это Python-библиотека, позволяющая организовать очередь плюс 
    асинхронное и распределенное исполнение задач. Со стороны Airflow все задачи делятся на пулы. 
    - Execution Date  В Airflow DAG имеет измерение Execution Date, т. е. в зависимости от расписания работы DAG’а создаются экземпляры 
    задачек на каждую Execution Date. И за каждую Execution Date задачи можно выполнить повторно — или, например, DAG может работать 
    одновременно в нескольких Execution Date
    
Связка с MlFlow + Airflow
    Конечно, сервис не живёт сам по себе в вакууме. Данный инструмент интегрирован в наш процесс продуктивизации, где за мониторинг и   
    состояние моделей отвечает MLflow, а переобучение и предикт происходит по расписанию (Airflow).
    
Сатанисты в юпитере делают свои грязные дела. Разрабатывают саму модель, экспериментируют. Метрики экспериментов логируются в mlflow. Туда же могут улетать все возможные артефакты, графики там, данные и т.п. Обучающие выборки логируются в dvs. Весь код в процессе написния коммитится в git, в ветку dev/
Когда модель готова к тестированию код переливается в ветку main, запускаются pipeline'ы, которые регистрируют версию модели в mlflow, и выкатывают DAG с моделью в airflow (через репозиторий в git).
Когда потестили, опять же переливается все и main в prod ветку, и катится уже в пром. airflow